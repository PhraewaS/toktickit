# Test DD และ Traceability ของ TokTickIT Lab 3

แผนนี้จัดทำขึ้นพร้อมกับ Sprint 3 contract ก่อนเริ่ม Lab 3 implementation โดยจัดกลุ่ม tests ตามความเสี่ยงและเชื่อมโยงกับ acceptance criteria ตามรูปแบบ Test DD ของ Lab sheet

| Test ID | Type | AC | สิ่งที่ทดสอบ | Expected Result | Automated test file | สถานะ |
|---|---|---|---|---|---|---|
| API-01 | API | AC-01/02 | valid, invalid และ inactive login รวมถึง safe response | valid active credentials ตอบ `200` พร้อม safe user data และ session; invalid/inactive login ตอบ `401 AUTHENTICATION_FAILED` และไม่สร้าง session | `server/tests/lab-03/auth.api.test.ts`, `server/tests/lab-03/auth.integration.test.ts` | Pass — server suite |
| API-02 | API | AC-03/04 | first-login gate, `PASSWORD_CHANGE_REQUIRED` บน protected routes, password change และ logout | gated session เรียก protected route อื่นตอบ `403 PASSWORD_CHANGE_REQUIRED`; auth exceptions ใช้ได้; password change สำเร็จปลด gate และ logout ทำให้ session ใช้ไม่ได้ | `server/tests/lab-03/auth.api.test.ts`, `server/tests/lab-03/auth.integration.test.ts`, `e2e/lab-03/authentication.spec.ts` | Pass — server/E2E |
| API-03 | API/security | AC-05/06 | authenticated requester ownership และ forbidden routes | Requester เข้าถึงได้เฉพาะ resource ของตน; resource ของผู้อื่นตอบ safe `404`; staff/admin routes ตอบ `403 ROLE_FORBIDDEN` | `server/tests/lab-03/authorization.api.test.ts`, `server/tests/lab-03/staff-operations.integration.api.test.ts` | Pass — server suite |
| API-03a | API/security | AC-09/15 | Administrator owner/priority permissions; Administrator mutation denial; non-Administrator Admin-route denial; retired Development Requester route | Administrator owner/priority ที่อนุญาตสำเร็จ; mutation ที่ไม่อนุญาตและ non-Administrator Admin routes ตอบ `403 ROLE_FORBIDDEN`; production legacy route ตอบ `410 DEVELOPMENT_REQUESTERS_RETIRED` | `server/tests/lab-03/authorization.api.test.ts`, `e2e/lab-03/staff-ticket-flow.spec.ts` | Pass — server/E2E |
| API-04 | API | AC-07 | queue query filters, sort และ pagination | IT Staff/Administrator ได้ `200` พร้อม data และ pagination; ผล filter/sort/page ถูกตรวจจริง; invalid query ตอบ `400 INVALID_QUERY` | `server/tests/lab-03/staff-queue.api.test.ts`, `server/tests/lab-03/staff-operations.integration.api.test.ts`, `e2e/lab-03/staff-ticket-flow.spec.ts` | Pass — server/E2E |
| API-05 | API | AC-08 | assignment, priority และ status matrix | allowed IT Staff operations ตอบ `200`; invalid input ตอบ `400`; unauthorized role ตอบ `403`; disallowed transition ตอบ `409 STATUS_TRANSITION_NOT_ALLOWED` | `server/tests/lab-03/staff-ticket-detail.api.test.ts`, `server/tests/lab-03/staff-operations.integration.api.test.ts` | Pass — server suite |
| API-05a | API/security | AC-08 | Administrator เป็น owner และ update IT Priority ได้; บังคับใช้ assignment/status/comment/note permissions; ทดสอบทุก allowed และ denied status transition | Administrator priority update และ owner eligibility สำเร็จ; Administrator mutation ที่ห้ามทำตอบ `403 ROLE_FORBIDDEN`; transition ที่อนุญาต/ปฏิเสธตรงกับ BR-10 | `server/tests/lab-03/authorization.api.test.ts`, `server/tests/lab-03/staff-ticket-detail.api.test.ts`, `e2e/lab-03/staff-ticket-flow.spec.ts` | Pass — server/E2E |
| API-05b | API | AC-10 | Requester resolved endpoint: success, idempotency, formal status ไม่เปลี่ยน, validation, ownership, role, session, first-login gate และ persistence errors | own-ticket ตอบ `200` ตาม response shape และเรียกซ้ำได้โดยไม่เปลี่ยน formal status; invalid/missing/forbidden/conflict cases ตอบ error code ที่กำหนด | `server/tests/lab-03/staff-operations.integration.api.test.ts` | Pass — server suite |
| API-06 | API/security | AC-09 | Requester own-ticket Public Comments, staff comments/notes, visibility และ append-only validation | Requester อ่าน/สร้าง Public Comments ใน ticket ของตนได้; Internal Notes ไม่รั่วไหล; role ที่ไม่อนุญาตและ invalid content ได้ safe error | `server/tests/lab-03/comments-notes.api.test.ts`, `server/tests/lab-03/staff-operations.integration.api.test.ts` | Pass — server suite |
| API-07 | API | AC-11 | admin list/search/filter/create/edit/deactivate/reset และ safety rules | Administrator operations ที่ถูกต้องตอบ `200/201`; non-Administrator ตอบ `403`; validation/conflict และ safety rules ใช้ status code ตาม contract | `server/tests/lab-03/users-admin.api.test.ts`, `e2e/lab-03/user-administration.spec.ts` | Pass — server/E2E |
| API-07a | API/security | AC-11 | deactivate revokes target sessions; next request with the old session returns `401 SESSION_INVALID`; malformed fields return `400 VALIDATION_ERROR`; valid state conflicts return `409 DUPLICATE_EMAIL` or `409 USER_UPDATE_CONFLICT` รวมถึงห้าม deactivate Administrator คนสุดท้าย | deactivation สำเร็จ revoke session เดิมทั้งหมด; malformed fields และ duplicate/state conflicts ตอบ code ที่ถูกต้อง; self-deactivation และ last-active-Administrator ตอบ `409 USER_UPDATE_CONFLICT` | `server/tests/lab-03/users-admin.api.test.ts`, `e2e/lab-03/user-administration.spec.ts` | Pass — server/E2E |
| API-08 | migration/regression | AC-05/12 | Lab 2 rows/tickets/attachments อยู่รอดหลัง migration และ seed reruns | `RequesterUser`/`requester_users` IDs, `Ticket.requesterId` foreign keys, tickets, attachments และข้อมูลเดิมยังครบ; migration/seed rerun ได้อย่างปลอดภัยและ idempotent | `server/tests/lab-03/migration-regression.api.test.ts` | Pass — server suite |
| UNIT-01 | unit/security | AC-01/03 | password hashing, password rules, session parsing และ transitions | hashes ไม่ใช่ plaintext; password rules และ session parsing ถูกต้อง; transition table ตรงกับ BR-10 | `server/tests/lab-03/auth.unit.test.ts`, `server/tests/lab-03/staff-ticket-detail.api.test.ts` | Pass — server suite |
| UI-01 | component | AC-01/02/13 | Login validation, busy, failure และ inactive-safe state | แสดง validation/busy/safe failure ถูกต้อง และไม่เปิด session สำหรับ invalid/inactive user | `client/tests/lab-03/Login.test.tsx` | Pass — client suite |
| UI-02 | component | AC-03 | mandatory password change และ continuation | gated user เห็น Change Password; เปลี่ยนสำเร็จแล้วไปยัง normal application route ได้; validation/failure แสดงชัดเจน | `client/tests/lab-03/ChangePassword.test.tsx` | Pass — client suite |
| UI-00 | component | AC-03/05/14/15 | role navigation, role landing และการไม่มี Development Requester selector | Requester/IT Staff/Administrator เห็นหน้าเริ่มต้นและ navigation ตาม role; authenticated shell ไม่ render selector หรือ Change requester | `client/tests/lab-03/AppRoleNavigation.test.tsx` | Pass — client suite |
| UI-03 | component | AC-07/13 | queue response mapping, open detail, filters, empty/no-results, forbidden และ failure | queue unwrap `{data:{items,pagination}}` เป็น `{items,pagination}` ได้ถูกต้อง; เปิด detail ได้; แสดง unassigned, empty/no-results, forbidden และ failure แยกกัน | `client/tests/lab-03/StaffTicketQueue.test.tsx` | Pass — client suite |
| UI-03a | API contract/component | AC-07/09 | client API envelope mapping สำหรับ queue และ requester comments | Client unwrap outer `data` ตาม API contract; Staff Queue ได้ `{items,pagination}` และ requester comments ได้รายการจาก `{data:{items}}` | `client/tests/lab-03/ApiContract.test.tsx` | Pass — client suite |
| UI-04 | component | AC-08/09/10/13 | staff detail operations, allowed transitions, attachments, requester resolution, comments และ notes | controls ที่ role มีสิทธิ์ใช้ได้; status แสดงเฉพาะ transition ที่อนุญาต; read-only/forbidden states, attachment download, comments/notes และ resolution indication ถูกต้อง | `client/tests/lab-03/StaffTicketDetail.test.tsx` | Pass — client suite |
| UI-05 | component | AC-11/13 | admin list/create/edit/reset/deactivate และ safety feedback | User Management แสดง success, validation, forbidden และ conflict feedback รวมถึง confirmation และ session-revocation message ก่อน deactivation | `client/tests/lab-03/UserManagement.test.tsx` | Pass — client suite |
| UI-06a | regression | AC-05/13 | Lab 2 Create Ticket component regression; authenticated requester flow อ้างอิง E2E-05 | component เดิมยังทำงานโดยไม่ render requester selector; authenticated identity และการสร้าง ticket ของ Requester ตรวจจริงใน E2E-05 | `client/tests/lab-02/CreateTicket.test.tsx`; authenticated flow: `e2e/lab-03/requester-flow.spec.ts` (E2E-05) | Pass — client component + E2E-05 |
| UI-06b | regression | AC-05/13 | Lab 2 My Tickets component regression; authenticated requester flow อ้างอิง E2E-05 | component เดิมยังแสดงรายการและ controls ได้; ownership ของ authenticated Requester และการเปิด ticket ของตนเองตรวจจริงใน E2E-05 | `client/tests/lab-02/MyTickets.test.tsx`; authenticated flow: `e2e/lab-03/requester-flow.spec.ts` (E2E-05) | Pass — client component + E2E-05 |
| UI-06c | regression | AC-05/13 | requester ticket detail และ ownership-safe states ยังใช้ได้ | เปิด detail ของ ticket ตนเองได้ และแสดง safe not-found/forbidden state สำหรับ resource อื่น | `client/tests/lab-02/RequesterTicketDetail.test.tsx` | Pass — client suite |
| UI-06d | regression | AC-05/13 | requester attachment controls ยังใช้ได้ | upload/download/remove attachments ที่อนุญาตได้ และ feedback/error states ถูกต้อง | `client/tests/lab-02/AttachmentSection.test.tsx` | Pass — client suite |
| UI-06e | regression | AC-05/13/14 | requester accessibility regression | labels, focus, semantics และ non-color-only feedback ผ่าน accessibility assertions | `client/tests/lab-02/Accessibility.test.tsx` | Pass — client suite |
| UI-06f | regression | AC-14 | requester visual token regression | Zen Green tokens และ visual conventions ยังตรงกับ Lab 2 | `client/tests/lab-02/ZenGreenStyles.test.tsx` | Pass — client suite |
| UI-06g | regression | AC-05/15 | authenticated Lab 3 client ไม่ render หรือเรียก Development Requester selector | production client ไม่มี selector/Change requester action และใช้ authenticated session identity เท่านั้น | `client/tests/lab-03/AppRoleNavigation.test.tsx` | Pass — client suite |
| API-09 | API/security | AC-01/04/12 | expired/invalid session ถูกปฏิเสธบน current-user และ protected routes | expired/invalid session ตอบ `401 SESSION_INVALID`; logout และ protected access หลัง revoke ใช้ต่อไม่ได้ | `server/tests/lab-03/auth.api.test.ts`, `server/tests/lab-03/auth.integration.test.ts` | Pass — server suite |
| STYLE-01 | UI style | AC-14 | Zen Green tokens, badges, focus และ read-only styling | tokens, badges, focus และ read-only/editable distinction ตรงตาม UI contract | `client/tests/lab-03/ZenGreenStyle.test.tsx` | Pass — client suite |
| RESP-01 | responsive | AC-14 | desktop/tablet/mobile ไม่มี page overflow | layout ไม่เกิด clipping, overlap หรือ horizontal overflow ในทุก viewport ที่กำหนด | `e2e/lab-03/responsive.spec.ts` | Pass — 3 viewports |
| E2E-01 | E2E | AC-01-06 | login, first-login และ logout | login/password-change/logout ทำงานครบด้วย authenticated session | `e2e/lab-03/authentication.spec.ts` | Pass — desktop; responsive projects skip ตาม design |
| E2E-02 | E2E/UI entry | AC-07-10 | IT Staff เปิด Queue/Detail และเห็น UI entry/actions ที่ตรวจใน browser | ตรวจเฉพาะ navigation, queue/detail entry และการแสดง operations ใน UI; mutation/authorization ครบชุดตรวจใน API-05a, API-06 และ E2E-06 ไม่อ้างว่าเป็น UI workflow ครบทั้งหมด | `e2e/lab-03/staff-ticket-flow.spec.ts` | Pass — desktop UI entry |
| E2E-03 | E2E/UI entry | AC-11 | Administrator เปิด User Management และเห็น create action | ตรวจเฉพาะ role landing และ User Management entry ใน browser; create/edit/reset/deactivate และ safety rules ตรวจใน API-07/API-07a และ E2E-07 | `e2e/lab-03/user-administration.spec.ts` | Pass — desktop UI entry |
| E2E-04 | E2E/security | AC-01/04/05/12/15 | invalid/inactive login, missing session, first-login gate และ logout invalidation | invalid/inactive ตอบ `401`; protected route ที่ยัง gated ตอบ `403 PASSWORD_CHANGE_REQUIRED`; หลัง logout ใช้ protected route ต่อไม่ได้ | `e2e/lab-03/authentication.spec.ts` | Pass — desktop API flow |
| E2E-05 | E2E regression | AC-05/09/10/15 | Requester create, own ticket detail, Public Comment และ resolved indication | Requester สร้าง/เปิด ticket ของตนเอง, เพิ่ม Public Comment และ resolved indication แบบไม่เปลี่ยน formal status ได้ | `e2e/lab-03/requester-flow.spec.ts` | Pass — desktop |
| E2E-06 | E2E/API | AC-07/08/09 | queue search/filter/sort, owner, priority, status, comments/notes และ attachment download | production routes ตอบ shape/status ตาม contract และไม่เกิด clipping ใน queue | `e2e/lab-03/staff-ticket-flow.spec.ts` | Pass — desktop API flow |
| E2E-07 | E2E/API/security | AC-11/12/13 | admin create/edit/reset/deactivate, validation/conflict, role boundary และ session revocation | mutation ที่อนุญาตสำเร็จ; malformed/conflict/forbidden/old-session cases ได้ status และ error code ตาม contract | `e2e/lab-03/user-administration.spec.ts` | Pass — desktop API flow |
| RESP-02 | responsive | AC-14/15 | major screens ของ Requester, IT Staff และ Administrator | Create Ticket, My Tickets, Staff Queue, Staff Detail, User Management และ navigation ไม่มี horizontal overflow หรือ control clipping | `e2e/lab-03/responsive.spec.ts` | Pass — 3 viewports |
| E2E-08 | E2E regression | AC-05/10/15 | authenticated Requester regression ที่แทน legacy Lab 2 selector flow | หลัง login และเปลี่ยนรหัสผ่านแล้ว Requester เข้า Create Ticket/My Tickets ได้ด้วย session identity และไม่ render Development Requester selector; behavior เดิมที่ยังอยู่ในขอบเขต Lab 3 เชื่อมกับ E2E-05 | `e2e/lab-03/requester-flow.spec.ts` | Replaced — test added; execution ต้องใช้ dedicated PostgreSQL/Chromium environment |

Final evidence ต้องบันทึก commands, test output, responsive screenshots, accessibility/style inspection และ final-main provenance ห้าม skip หรือ disable tests ที่จำเป็น Authorization cases ต้องเป็น explicit test cases ไม่ใช่มีเพียง documentation rows โดยต้องครอบคลุม: Requester Public Comment GET/POST ต้องสำเร็จเฉพาะ ticket ของ Requester เอง; การเข้าถึง comment ของผู้อื่นต้อง assert safe `404`; Requester resolved ต้อง assert `200` response shape, idempotency, formal status ที่ไม่เปลี่ยน และกรณี `400/401/403/404/500` ทั้งหมด; first-login-gated session ต้อง assert `403 PASSWORD_CHANGE_REQUIRED` บน representative protected requester, staff และ admin routes ขณะที่ auth exceptions ทั้งสามยังใช้ได้; Administrator owner eligibility และ IT Priority update ต้อง assert success; Administrator assignment/status/comment/note mutations ต้อง assert `403 ROLE_FORBIDDEN`; Administrator deactivation ต้อง assert target sessions ถูก revoke และ old-session request ถัดไปตอบ `401 SESSION_INVALID`; malformed admin update fields ต้อง assert `400 VALIDATION_ERROR`; valid duplicate/state conflicts ต้อง assert `409 DUPLICATE_EMAIL`/`409 USER_UPDATE_CONFLICT`; non-Administrator Admin routes ต้อง assert `403 ROLE_FORBIDDEN`; missing และ invalid sessions ต้อง assert `401`; และ complete BR-10 transition table ต้อง assert ทั้ง allowed และ denied transitions

Final report ต้องระบุ command ที่ใช้จริงและ file path ของทุกไฟล์ที่ execute ห้ามใช้ wildcard path เป็น evidence Regression entries ใช้ explicit paths ตาม UI-06a ถึง UI-06g และ E2E-01 ถึง E2E-08 ตามที่ระบุด้านบน Legacy Lab 2 selector flow ไม่ถูกนำมารันซ้ำ เพราะ Lab 3 ยกเลิก Development Requester selector และใช้ authenticated session identity; E2E-05 และ E2E-08 จึงเป็น regression coverage ที่ใช้แทน behavior เดิมภายในขอบเขต Lab 3

ผลการตรวจสอบ revision ก่อนหน้านี้รองรับสถานะ `Pass` ของ Server/Client ตามผลรันจริงที่บันทึกไว้ ส่วน revision นี้เพิ่ม Playwright test และ config guard ใหม่ โดย `playwright --list` พบ 42 tests ใน 6 files ครบทั้ง 3 projects แต่การรันจริงล่าสุดหยุดระหว่าง seed เนื่องจาก PostgreSQL local ถูก Application Control policy บล็อก `pg_trgm.dll`; จึงยังไม่อ้าง Playwright เป็น Pass จนกว่าจะรันซ้ำใน dedicated environment ที่พร้อม รายการ E2E-02 และ E2E-03 ระบุเฉพาะ UI entry/role landing; การ mutation และ authorization ของ Staff/Administrator อ้างอิง production API tests ตาม path ที่ระบุ ไม่ถือว่าเป็น UI workflow ครบทุก operation และ E2E-08 ใช้ authenticated Requester regression ของ Lab 3 แทน legacy selector flow อย่างมีเหตุผล
